# JavaScript - Warm up

This project contains basic JavaScript exercises covering variables, arguments, loops, functions, and modules, part of the Higher Level Programming curriculum.

## Requirements

- All files are interpreted/compiled on Ubuntu using Node.js
- All files must be executable
- Code must pass `semistandard` linting
